import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.testkeystore',
  appName: 'test-key-store',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
